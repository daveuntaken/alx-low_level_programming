#include <stdio.h>

int main(void)
{
  char c;
  int_isupper(int c);

  if c >= 'A' && c <= 'Z'
	    {
	      printf("1");
	      }
  else if c >= 'a' && c <= 'z'
		 {
		   printf("0");
		 }
  return 0;
